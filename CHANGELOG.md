# 更新日志

## v2.8.6

### 新功能

- **后备海报外联代理**：后备搜索可将受防盗链或地区限制的海报缓存为可访问地址，并支持按 Token 授权使用。
- **弹幕源双向版本控制**：同时校验弹幕源要求的最低服务器版本与服务器要求的最低源包版本；新增统一的 `PACKAGE_VERSION` 批次号，与各源自身 `__version__` 分离。
- **日志检索能力**：历史日志接口新增关键词过滤、偏移量分页和剩余数据标识，前端支持分页加载。
- **任务性能统计**：新增任务流程和步骤级耗时统计 API，支持执行次数、平均耗时、步骤耗时、成功率及百分位数据查询；任务管理页面新增性能统计面板，并支持按时间范围查看。

### 修复

- **原始响应日志统一**：各弹幕源统一通过 `BaseScraper._log_raw_response()` 完整写入响应，自动标注源名称，并继续受各源日志开关控制。
- **搜索日志乱序**：日志刷新改在线程池执行，并在返回搜索结果前等待完成，保证各源日志块先于计时报告输出且不阻塞事件循环。
- **后备海报与日志显示**：完善后备海报降级处理，修复历史日志切换、分页及长内容显示问题。
- **主页搜索接口 500**：修复完整搜索流程结束时遗漏返回值，避免 FastAPI 响应模型校验 `None` 失败。
- **性能统计事务**：修复性能统计数据随主业务事务回滚的问题，统计记录改用任务级会话独立写入，避免统计异常影响已完成的业务操作。

### 优化

- **公共模块整理**：弹幕解析器迁移至 `src.utils` 统一复用，并清理搜索、缓存、日历、诊断、媒体服务器等模块的重复导入。
- **域名与图片处理**：统一自定义域名获取方式，兼容 HTTP/HTTPS 场景并减少重复配置读取。
- **版本错误提示**：加载失败时明确区分服务器版本、源包批次和单源版本，未声明 `PACKAGE_VERSION` 的旧格式或本地源仅告警、不阻断加载。
- **任务流程与会话管理**：完善导入、刷新、Webhook 和自动导入任务的性能埋点，通过 `ContextVar` 注入任务级会话工厂，并提取任务异常类型以解决循环依赖。
- **缓存与文件 I/O 性能**：优化搜索、弹幕、分集和任务统计相关缓存访问及数据库查询，减少重复读写；改进文件处理和缓存回退路径，提升高并发场景下的响应稳定性。
- **性能面板交互**：性能统计卡片统一采用 Ant Design 内嵌卡片样式，默认展开步骤详情，并增加定时自动刷新和手动刷新能力。

### 兼容性说明

- 历史日志内容接口默认读取数量由 500 调整为 200，返回值改为包含 `lines`、`hasMore` 和 `total` 的分页结构，并新增 `keyword`、`offset` 参数。