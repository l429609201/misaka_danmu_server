# 更新日志

## v2.8.7

### 新功能

- **弹幕来源标签压缩**：新增 `danmakuSourceTagEnabled` 和 `danmakuSourceTagAlias` 配置项。开启后，弹幕 XML 的来源标签可由完整 provider 名称（如 `[bilibili]`）压缩为别名（默认 `[0]`），减少 XML 文件体积；关闭时保持原始 provider 标签兼容行为。
- **新增类弹弹Play版本号接口**：新增兼容 dandanplay API v2 的 `GET /{token}/api/v2/version` 接口，返回服务端名称、当前应用版本号及 ISO 8601 格式服务器时间。

### 修复

- **弹幕源更新循环重启**：全量弹幕源包缺少 `package.json` 或 `versions.json` 时，使用下载前获取的远程 `package.json` 作为版本信息兜底，补全临时目录及持久化备份中的版本、哈希和最低服务端版本信息，避免备份校验失败后反复重启。
- **Compose 镜像标签更新不生效**：更新流程改为显式拉取目标镜像，并在 Compose 重建前将目标镜像对齐到 Compose 当前定义的镜像名；同时显式停止、删除并强制重建服务，修复从 `:test` 更新到 `:latest` 时内容未更新的问题。
- **Docker socket 权限处理**：启动脚本不再通过放宽 socket 权限的方式授权，改为动态读取 socket GID、创建/复用对应用户组并将 `appuser` 加入附加组；配合 gosu 正确继承附加组，恢复 WebUI 一键更新和重启能力。

### 优化

- **一键更新进度显示**：前端将连续的“下载中”消息原地更新为单条进度记录，避免下载过程中日志区域持续刷屏；其他阶段提示和错误消息仍按行追加。
- **弹幕 XML 来源信息**：保存弹幕时支持从弹幕源 `handled_domains` 自动获取来源官网作为 `<chatserver>`，并将来源标签压缩配置统一应用于首次导入、增量导入、单集刷新、批量刷新和全量刷新。
- **异步文件写入**：弹幕 XML 生成及文件写入相关的 CPU/IO 密集操作放入线程池执行，减少对事件循环的同步阻塞。
- **调整docker套接字授权**：改为对容器运行用户附加docker权限组。

### 兼容性说明

- 新增版本接口兼容 dandanplay API v2 的标准响应字段。
- `danmakuSourceTagEnabled` 默认关闭，已有 XML 的来源标签格式和默认行为不变。
- Compose 更新仍以用户 Compose 文件中的服务定义为准；本次修复确保目标镜像内容被正确应用，不会擅自修改用户的 Compose 配置文件。

