# 更新日志

## v2.8.5

### 新功能

- **AI 接口兼容增强**：支持 OpenAI Responses API，并可自动兼容 Chat Completions；新增 AI 请求超时配置，适配新模型的 Token 参数及推理日志记录。
- **弹幕源兼容性检测**：新增弹幕源加载检查接口和前端提示，可展示服务器版本不兼容、全局跳过及单源跳过原因。
- **主题与界面风格**：扩充页面风格选择，新增可配置图源的二次元壁纸主题，并完善玻璃、磨砂等主题的卡片表现。
- **任务结果消息**：后台任务可返回具体业务结果，任务历史与通知能够展示实际下载数量等完成信息。

### 修复

- **修复超时限制熔断问题**：修复超时熔断无法调整的BUG。
- **修复多源搜索的情况下，无法下载的问题**：修复在本地有源的情况下，返回的epid不为多源的epid的问题。
- **弹幕源更新与下载**：统一校验 `min_server_version` / `min_fetchable_version`，兼容 `package.json`、`versions.json` 及不同包格式；修复平台键、哈希和更新时间读取错误，并在部署前完成 `.so/.pyd` 兼容性预检。
- **更新失败保护**：补充失败回滚、备份恢复和全量失败后的增量降级机制，避免不兼容源被部署、版本回退及自动更新无限重启。
- **下载进度与 SSE**：修复校验阻塞事件循环、失败时进度误显示完成、失败/取消后加载状态未关闭，以及任务状态枚举错误导致 SSE 无限推送的问题。
- **任务状态准确性**：导入、预下载等业务失败改为 `TaskFailed`，任务历史、进度条和失败通知不再错误显示为成功。
- **搜索关键词处理**：括号归一化改为仅移除括号符号并保留其中内容，避免《【我推的孩子】》等标题被错误截断。
- **日志查看**：修复历史日志切换后滚动位置异常和长行布局问题；元数据响应日志不再显示 Unicode 转义字符。
- **通知渠道**：修复 Telegram MarkdownV2 链接清洗及图片发送失败问题，失败时可降级为纯文本发送。

### 优化

- **大日志读取性能**：采用反向读取并限制单行及总读取字节数，降低大文件日志的内存占用，并增加截断提示。
- **通知配置布局**：Telegram、Server酱和企业微信配置支持分组展示，提升复杂表单的可读性。
- **响应式界面**：统一多个页面的卡片与表格样式，优化移动端嵌套卡片、任务列表及设置页面显示。


