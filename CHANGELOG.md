# 更新日志

## v2.8.8

### 新功能

- **弹幕源动态部署检测**：新增弹幕源部署状态检测能力，根据运行环境和已加载模块判断是否需要延迟覆盖文件或重启容器。
- **安全部署策略**：支持将下载内容先部署到临时目录，校验通过后再写入备份目录和运行目录，降低原生 `.so` 文件更新过程中的运行时风险。

### 修复

- **修复弹幕源下载后，会直接覆盖当前运行目录**：避免直接覆盖正在被进程加载的 `.so` 文件，并将高风险运行目录覆盖操作延后到持久化备份完成之后。
- **修复全量替换更新后无限重启和版本回退问题**：统一版本时间字段为 `updated_at`，确保新版弹幕源、版本清单和备份目录同步落盘，重启后能够正确恢复目标版本。
- **修复搜索接口报错的问题**：修复主搜索接口报错的问题。

### 重构

- **弹幕源加载校验文件重构**：引入 `scraper_manifest.json` 作为弹幕源版本、分支、更新时间、服务器最低版本、文件路径和哈希信息的统一权威来源，逐步替代 `package.json` 与 `versions.json`。